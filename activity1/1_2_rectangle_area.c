#include <stdio.h>

int main() {
    // Variables
    int length = 20, width = 4;

    // Display calculated area
    printf("Area of %d x %d rectangle: %d \n", length, width, length * width);

    // Return
    return 0;
}
